# 界面约定

这是游戏内房屋编辑器。用户需要看清场景、找物件、调整位置，并验证门和通路。

- 中性深灰底色，暖色仅用于主要操作、焦点和当前标签。无发光、装饰渐变或粗彩色卡片边线。
- 中文标题直接说明用途。不添加装饰英文、无含义编号或品牌占位侧栏。
- 功能正文 13–14px，辅助信息 12px，标题 16–20px。保持字重克制和足够对比度。
- 房屋采用可搜索列表；家具采用缩略图目录。分组用间距和分隔线，不用层叠卡片。
- 物件与房屋设置分页；进入场景时工具面板让出空间，保留必要快捷键提示。
- 实体门是接触驱动的摆动门扇，不是传送点。出入口必须单独创建并配对。
- 验证默认隐藏、F2/F6/F7、长中文名称、空列表、导入失败、禁用状态、720p/1080p、键盘焦点，以及旧 NUI 回调。
