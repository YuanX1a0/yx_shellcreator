# 家具目录说明

当前版本只保留项目原有的 GTA V 家具与建筑目录，以及 `yx_shellcreator` 自带的建筑模型。没有附加外部家具包、房屋资源或第三方模型文件。

## 内容

| 内容 | 数量 |
| --- | ---: |
| 目录条目 | 358 |
| 当前可新建条目 | 356 |

目录中的两扇自制门 `yx_door_wood`、`yx_door_modern` 已从新建列表隐藏，但保留旧房屋运行所需的模型、碰撞和元数据。更新不会自动删除数据库中的旧物件；如果旧房屋里不再需要它们，可以在场景中选中后按 `DEL` 删除。

所有条目支持模型名和中文名搜索，每页 40 项，可以完整翻页。没有对应缩略图的条目会显示文字卡片和真实模型名，不会用其他物件图片冒充。模型是否可用会按当前 FiveM 游戏构建标记；灰色模型不能放置，避免静默替换成错误物件。

## 安装和使用

1. 停止旧版 `yx_shellcreator`，备份数据库与房屋导出文件。
2. 将新的 `yx_shellcreator` 文件夹复制到 `resources/[standalone]/`。
3. 配置并启动：

   ```cfg
   ensure oxmysql
   ensure yx_shellcreator
   ```

4. 重新连接游戏，输入 `/shellcreator`，进入房屋并打开建造模式。
5. 在左侧按类别或关键词筛选后点击物件即可放置。
