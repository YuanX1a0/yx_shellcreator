# 功能验收清单

## 资源结构

- [x] 资源名为 `yx_shellcreator`
- [x] standalone，不引用 QB 或 ESX
- [x] 仅依赖 oxmysql，不影响 standalone
- [x] 使用 OneSync routing bucket 隔离房屋实例
- [x] JavaScript 客户端
- [x] C# 服务端
- [x] 原生 HTML/CSS/JS NUI
- [x] 中性深灰 UI、可搜索房产列表、按需展开配置栏、物件/房屋设置分页与场景编辑双侧栏
- [x] 透明场景背景，深色原生控件仅作用于输入框；默认隐藏、不覆盖游戏画面
- [x] 1080p 与较窄分辨率自适应布局
- [x] 除 `fxmanifest.lua` 外没有 Lua 文件
- [x] 不加载其他房屋资源的脚本
- [x] 不包含联网版本检查、webhook 或图片上传逻辑

## 房屋创建与管理

- [x] 所有人可以创建
- [x] 所有人可以查看
- [x] 所有人可以修改
- [x] 所有人可以删除
- [x] 使用当前人物位置和朝向创建入口
- [x] 可把当前位置的 GTA 原版地图室内直接创建为独立房产
- [x] 世界入口标记与 `E` 交互
- [x] 独立 routing bucket
- [x] 多层嵌套房屋上下文；离开内层后恢复上一层实例与编辑能力
- [x] `/fixshell`、`/fixloc` 室内卡位与 routing bucket 修复
- [x] 进入房屋时的 OBJ 预加载、分批生成、碰撞等待和最短加载画面
- [x] GTA 原生室内模板
- [x] 三套可自动请求 IPL 的行政公寓模板
- [x] 空白建造空间
- [x] 空白空间专用斜俯视相机
- [x] 自定义 shell 模型
- [x] 自定义室内出生点
- [x] 自定义室内出口点
- [x] 每套房屋最多 16 个配对出入口，外部和屋内位置可分别设置、单独删除
- [x] 按标记选择配对传送点；命令离开返回本次入点；修复优先使用本次入口的屋内点
- [x] 出入口与门模型完全独立；点位持久化并支持 JSON 相对坐标导入导出
- [x] 每套房屋独立天气与固定时间，可分别跟随服务器
- [x] 环境设置 oxmysql 持久化、同房屋同步、离开/资源停止时解除本地覆盖
- [x] 嵌套房屋切换时应用对应房屋环境
- [x] 本地环境覆盖事件供第三方天气资源协同使用
- [x] 导出当前房屋 JSON 到服务器 exports 目录，支持界面复制与浏览器下载辅助入口
- [x] 创建页选择 JSON 文件或粘贴 JSON 后快速导入并自动进入建造模式
- [x] 保留布局、点位、原生家具修改、门状态与环境；不打包模型文件
- [x] 导入重新生成房屋和物件 ID，不覆盖原有房屋
- [x] 相对坐标迁移；原版地图房产保留原室内地图坐标
- [x] JSON 格式、版本、坐标、模型名、天气时间和物件数量校验
- [x] 导入数据库事务、失败回滚、文件大小限制及操作冷却
- [x] JSON 导入/导出使用限速网络传输

## 建造与装修

- [x] 无价格家具目录
- [x] 模型搜索与分类
- [x] 356 个可选原版目录条目，无价格或框架代码
- [x] 中文/模型名搜索、每页 40 项完整分页
- [x] 无缩略图的家具采用文字列表，不使用不对应的预览图片
- [x] 全目录模型可用性检测、可选资源未启动提示与检测缓存
- [x] 普通编辑刷新不再重复传输整份家具目录
- [x] 任意模型名输入
- [x] 物件创建
- [x] 独立自由相机并冻结玩家控制
- [x] 鼠标光标射线选择
- [x] 场景悬停高亮与直接点选（无需列表盲选）
- [x] 原生三维 Gizmo 拖拽
- [x] 三轴拖拽位移
- [x] 三轴拖拽旋转
- [x] 自由相机加速与精细移动
- [x] 空白空间建造网格
- [x] 自带模型与碰撞的 2.5 米模块化地板、实墙、门洞、实体门和楼梯
- [x] 3 米层高、5 米进深、20 级/15 厘米台阶，带楼梯标记和连续支撑面
- [x] 两套随资源打包的 270° 旋转楼梯（橡木/混凝土），含护栏与嵌入式碰撞
- [x] 保留三个原版旋梯选项；依赖对应游戏构建与内饰资源，不保证脱离原地图可用
- [x] 门按脚本物件方式创建；新建物件在写入数据库前检查实体生成
- [x] `/yxmodelcheck` 本地模型生成检查、F8 诊断、目录标记未注册模型
- [x] 楼梯静止时不再每帧移动碰撞体，变换时才同步
- [x] 两扇自制门撤下新增目录；保留旧房屋模型兼容与十二个 GTA 原版门条目
- [x] 走向门扇时接触驱动双向推开，离开自动缓回，门洞有人时保持已打开的门
- [x] 临时推门同房间同步并限频，不逐帧写数据库；常开设置单独持久化
- [x] 建造面板“试开门”与“常开”控件；静态门洞和可推门扇明确区分
- [x] 建造模式自动闭门，避免打开后的动画坐标污染 Gizmo 保存坐标
- [x] 原大型平台、坡道、集装箱和整栋模型保留在独立分类
- [x] 鼠标网格射线定位与半透明预览
- [x] 左键连续铺设
- [x] 地板格内吸附、墙体格边吸附、按部件 45°/90° 步进旋转与拖拽后重新吸附
- [x] 普通物件 45° 软吸附与 0°/45°/90°/180° 精确角度按钮
- [x] 斜俯视平移、环绕与滚轮缩放
- [x] 贴地
- [x] 复制
- [x] 自动保存和手动保存
- [x] 删除
- [x] DEL 删除快捷键
- [x] 创建、删除和变换的撤销/重做
- [x] 原生公寓、旅馆及当前位置地图房产中可独立命中家具的接管、编辑、隐藏和恢复
- [x] 创建房屋后自动进入并开启建造模式
- [x] F6/F2 建造状态与 NUI 焦点冲突修复
- [x] 同一房屋内玩家实时同步物件变化

## 已删除范围

- [x] 购买/出售
- [x] 租赁/房贷/账单
- [x] 钥匙/门锁/权限组
- [x] 房主与职业限制
- [x] 家具收费与退款
- [x] 仓库/衣柜/车库
- [x] 抢劫/种植/任务
- [x] 银行/电话/背包适配
- [x] 外部 webhook、上传和更新检查

## 1.10.0 验证范围

- [x] C# 服务端 Release 编译
- [x] 357 项完整分页可达、大小写去重、原始数据不变、隐藏门保留元数据、中文搜索与模型状态缓存模拟
- [x] 客户端与界面 JavaScript 语法、注册入口和已有门/楼梯逻辑回归
- [x] JSON 导出/导入往返、坐标迁移、原生家具记录、数据边界自动化测试
- [x] 本地环境覆盖、恢复、嵌套切换与资源停止的模拟运行测试
- [x] 推门逻辑模拟：双向接触、旋转门轴、静止/载具/建造状态保护、碰撞停止速度后的行走意图、门洞占用保护、自动回位与试开坐标隔离
- [x] 附加出入口配对、主入口出生点区分、删除 ID 拦截、JSON 坐标迁移与上限校验
- [x] 浏览器预览：搜索、配置栏、门试开/常开提交、环境表单、出入口增改删确认、导出弹窗、文件导入与透明背景
- [ ] 真实 FiveM / oxmysql 联机环境：角色推门与碰撞手感、双人迎面推门、点位往返、多人同步、数据库迁移、重启与第三方天气资源协同验收

门采用脚本控制的接触式摆动，不是 GTA 原生物理铰链或角色手部动画。以上“已完成”表示实现或列出的本地验证通过，不代表所有游戏场景都已实测。
